<%= turbo_frame_tag "cart" do %>
  <div class="dropdown-cart" id="cart-menu">
    <% if @cart.orderables.count > 0 %>
      <div class="dropdown-cart__item">
        <%= render "carts/orderables" %>
      </div>
      <hr>
      <p>Total: <%= @cart.total %></p>
      <div class="dropdown-cart__buttons">
        <%= link_to "Create order", new_order_path, data: { turbo_frame: "_top" }, class:"dropdown-cart__btn" %>
        <%= link_to "View cart", cart_path, data: { turbo_frame: "_top" }, class:"dropdown-cart__btn" %>
      </div>
 <% else %>
      <p>Your cart is empty</p>
    <% end %>
  </div>
<% end %>
